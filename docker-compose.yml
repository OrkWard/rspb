version: "3.8"

services:
  rspb:
    build: .
    image: rspb:latest
    restart: unless-stopped
    environment:
      - RUST_LOG=${RUST_LOG:-info}
    ports:
      - "${PORT:-3030}:3030"
    volumes:
      - rspb-data:/app/db

volumes:
  rspb-data:
